<h1>Friends List</h1>

<div id="friends">
  <% @friends.each do |friend| %>
    <%= render friend %>
    <p>
      <%= link_to "Show this friend", friend if friend.user == current_user%>
    </p>
  <% end %>
</div>

<%= link_to "New friend", new_friend_path %>


